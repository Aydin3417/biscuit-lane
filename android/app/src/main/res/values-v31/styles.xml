<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12 draws the splash itself and ignores the window
     background, so the same two things are said again in the terms that
     release understands. Without this the launch is the system default:
     the launcher icon on the theme's background, which is not the
     colour the game opens on. -->
<resources>
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
